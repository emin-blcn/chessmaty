[package]
name = "rust"
version = "1.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
godot = { version = "0.5.5", features = ["experimental-threads"] }
shakmaty = { version = "0.30.1", features = ["variant"] }